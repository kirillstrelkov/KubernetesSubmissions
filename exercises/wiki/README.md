# Wikipedia with init and sidecar

To apply deploment run:

```bash
make
```

For more information check [./Makefile](./Makefile) and [./dep.yaml](./dep.yaml)

> Use port forward for `wiki-svc` to open in browser
